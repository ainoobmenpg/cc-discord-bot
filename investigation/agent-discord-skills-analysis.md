# agent-discord-skills 詳細分析

## 概要

agent-discord-skillsは、Claude Code用のDiscord APIスキルセット。 Discord API v10ベースで、メッセージ送信・取得、チャンネル作成・一覧・管理機能を提供する。

## プロジェクト構造

```
agent-discord-skills/
├── discord-send-message/      # メッセージ送信スキル
├── discord-get-messages/      # メッセージ取得スキル
├── discord-create-channel/    # チャンネル作成スキル
├── discord-list-channels/     # チャンネル一覧スキル
├── discord-manage-channel/    # チャンネル管理スキル
├── package.json               # Node.jsパッケージ設定
├── README.md                  # プロジェクト説明
└── LICENSE                    # MITライセンス
```

## スキル構造

### 共通フォーマット

すべてのスキルは以下の構造に従う：

```
skill-name/
├── SKILL.md       # メインのスキル定義（YAML frontmatter + Markdown）
└── examples.md    # 使用例とシナリオ
```

### SKILL.mdの構造

#### YAML frontmatter

```yaml
---
name: discord-send-message
description: Send messages to Discord channels via the Discord API. Use this skill when the user wants to send text messages, notifications, or formatted content to a Discord channel.
---
```

**必須フィールド**:
- `name`: スキル名（一意識別子）
- `description`: スキルの説明と使用タイミング

#### Markdownセクション

1. **タイトル**: `# Discord Send Message`
2. **When to Use This Skill**: 使用タイミングの具体的な例
3. **Prerequisites**: 必要な環境変数と権限
4. **Instructions**: Claude Codeへの具体的な手順指示
5. **Validation Rules**: バリデーションルール
6. **Error Handling**: 一般的なエラーと対処法
7. **Security Notes**: セキュリティ上の注意点
8. **Examples**: examples.mdへの参照
9. **API Reference**: Discord APIのエンドポイント情報

### examples.mdの構造

1. **具体的な使用例**: ユーザーリクエスト → Skill Actions → 期待されるレスポンス → ユーザーフィードバック
2. **エラーハンドリング例**: 一般的なエラーシナリオ
3. **ベストプラクティス**: 実装上の推奨事項
4. **リファレンス**: カラーコード、権限値など

## 各スキルの詳細分析

### 1. discord-send-message

#### 目的
Discordチャンネルにメッセージを送信する。プレーンテキスト、マークダウン、embedをサポート。

#### APIエンドポイント
```
POST https://discord.com/api/v10/channels/{CHANNEL_ID}/messages
```

#### 必要な環境変数
- `DISCORD_BOT_TOKEN`: Discordボットトークン

#### 必要な権限
- Send Messages (2048)
- Read Message History (65536)
- View Channels (1024)

#### サポートするメッセージ形式

1. **プレーンテキスト**:
```json
{
  "content": "Hello from Claude Code!"
}
```

2. **マークダウン**:
```json
{
  "content": "**Bold text** *Italic text* `code` [Link](https://example.com)"
}
```

3. **Embed**:
```json
{
  "embeds": [{
    "title": "Notification",
    "description": "This is an embed message",
    "color": 3447003,
    "fields": [
      {
        "name": "Field Name",
        "value": "Field Value",
        "inline": false
      }
    ]
  }]
}
```

#### バリデーションルール
- メッセージコンテンツ: 最大2000文字
- Embed説明: 最大4096文字
- Embedタイトル: 最大256文字
- 合計embedサイズ: 最大6000文字
- チャンネルID: 数値（snowflake形式）

#### エラーハンドリング
- **401 Unauthorized**: 無効なボットトークン
- **403 Forbidden**: 権限不足またはボットがサーバーにいない
- **404 Not Found**: チャンネルが存在しない
- **400 Bad Request**: 無効なメッセージコンテンツ

#### レート制限
- 5メッセージ/5秒/チャンネル

---

### 2. discord-get-messages

#### 目的
Discordチャンネルからメッセージを取得する。ページネーションとフィルタリングをサポート。

#### APIエンドポイント
```
GET https://discord.com/api/v10/channels/{CHANNEL_ID}/messages?limit=50
```

#### 必要な環境変数
- `DISCORD_BOT_TOKEN`: Discordボットトークン

#### 必要な権限
- Read Message History (65536)
- View Channels (1024)

#### クエリパラメータ
- `limit`: 取得するメッセージ数（デフォルト: 50、最大: 100）
- `before`: このメッセージIDより前のメッセージを取得
- `after`: このメッセージIDより後のメッセージを取得
- `around`: このメッセージID付近のメッセージを取得

#### メッセージオブジェクト構造
```json
{
  "id": "1234567890123456789",
  "channel_id": "123456789012345678",
  "author": {
    "id": "987654321098765432",
    "username": "Username",
    "discriminator": "0000",
    "avatar": "avatar_hash"
  },
  "content": "Message text content",
  "timestamp": "2025-10-20T12:00:00.000000+00:00",
  "edited_timestamp": null,
  "tts": false,
  "mention_everyone": false,
  "mentions": [],
  "mention_roles": [],
  "attachments": [],
  "embeds": [],
  "reactions": [],
  "pinned": false,
  "type": 0
}
```

#### ページネーション戦略
1. 最初のバッチ: `?limit=100`
2. レスポンスから最も古いメッセージIDを取得
3. 次のバッチ: `?before={oldest_id}&limit=100`
4. 必要な数だけ繰り返す

#### エラーハンドリング
- **401 Unauthorized**: 無効なボットトークン
- **403 Forbidden**: 権限不足
- **404 Not Found**: チャンネルが存在しない
- **400 Bad Request**: 無効なlimitパラメータ

#### レート制限
- 5リクエスト/5秒/チャンネル

---

### 3. discord-create-channel

#### 目的
Discordサーバーに新しいチャンネルを作成する。テキスト、ボイス、カテゴリ、アナウンス、ステージチャンネルをサポート。

#### APIエンドポイント
```
POST https://discord.com/api/v10/guilds/{GUILD_ID}/channels
```

#### 必要な環境変数
- `DISCORD_BOT_TOKEN`: Discordボットトークン

#### 必要な権限
- Manage Channels (16)

#### チャンネルタイプ

| Type | Value | Description |
|------|-------|-------------|
| GUILD_TEXT | 0 | Text channel |
| GUILD_VOICE | 2 | Voice channel |
| GUILD_CATEGORY | 4 | Category |
| GUILD_ANNOUNCEMENT | 5 | Announcement channel |
| GUILD_STAGE_VOICE | 13 | Stage channel |

#### チャンネルプロパティ

```json
{
  "name": "channel-name",           // 必須、2-100文字
  "type": 0,                        // チャンネルタイプ
  "topic": "Channel topic",         // オプション、最大1024文字
  "parent_id": "123456789012345678", // オプション、カテゴリID
  "nsfw": false,                    // オプション
  "position": 0,                    // オプション
  "permission_overwrites": []       // オプション
}
```

#### チャンネル名バリデーション
- 2-100文字
- 小文字、数字、ハイフン、アンダースコアのみ
- スペース禁止（ハイフンを使用）
- 特殊文字・絵文字禁止

**有効な名前**: `general`, `general-chat`, `voice-room-1`
**無効な名前**: `General Chat`（スペースと大文字）, `chat!`（特殊文字）, `a`（短すぎ）, `café`（アクセント文字）

#### エラーハンドリング
- **400 Bad Request**: 無効なチャンネル名またはパラメータ
- **401 Unauthorized**: 無効なボットトークン
- **403 Forbidden**: Manage Channels権限不足
- **404 Not Found**: サーバーが存在しない

#### レスポンスオブジェクト
```json
{
  "id": "987654321098765432",
  "type": 0,
  "guild_id": "123456789012345678",
  "position": 0,
  "permission_overwrites": [],
  "name": "new-channel",
  "topic": null,
  "nsfw": false,
  "last_message_id": null,
  "parent_id": null
}
```

---

### 4. discord-list-channels

#### 目的
Discordサーバーのすべてのチャンネルを一覧表示する。タイプ別、カテゴリ別のフィルタリングをサポート。

#### APIエンドポイント
```
GET https://discord.com/api/v10/guilds/{GUILD_ID}/channels
```

#### 必要な環境変数
- `DISCORD_BOT_TOKEN`: Discordボットトークン

#### 必要な権限
- View Channels (1024)

#### チャンネルタイプ

| Type | Value | Label | Emoji |
|------|-------|-------|-------|
| GUILD_TEXT | 0 | Text | 💬 |
| GUILD_VOICE | 2 | Voice | 🔊 |
| GUILD_CATEGORY | 4 | Category | 📁 |
| GUILD_ANNOUNCEMENT | 5 | Announcement | 📢 |
| GUILD_STAGE_VOICE | 13 | Stage | 🎙️ |
| GUILD_FORUM | 15 | Forum | 💭 |

#### チャンネルオブジェクト構造
```json
{
  "id": "123456789012345678",
  "type": 0,
  "guild_id": "987654321098765432",
  "position": 0,
  "permission_overwrites": [],
  "name": "general",
  "topic": "General discussion",
  "nsfw": false,
  "last_message_id": "111222333444555666",
  "parent_id": null
}
```

#### フィルタリング方法

**タイプ別**:
```bash
jq '.[] | select(.type == 0)'  # テキストチャンネルのみ
```

**名前別**:
```bash
jq '.[] | select(.name | contains("general"))'
```

**カテゴリ別**:
```bash
jq '.[] | select(.parent_id == "category_id_here")'
```

#### 出力形式

**シンプルリスト**:
```
Channels in server 123456789012345678:
- general (text)
- announcements (announcement)
- voice-chat (voice)
- Community (category)
```

**階層リスト**:
```
📁 Community (Category)
  💬 general-chat (Text)
  💬 help (Text)
  🔊 Voice Room 1 (Voice)
```

**CSVエクスポート**:
```csv
Channel ID,Name,Type,Category,Topic
111222333444555666,general,text,Community,General discussion
```

---

### 5. discord-manage-channel

#### 目的
Discordチャンネルのプロパティを変更・削除する。名前変更、トピック更新、権限変更、移動、削除をサポート。

#### APIエンドポイント
```
PATCH https://discord.com/api/v10/channels/{CHANNEL_ID}
DELETE https://discord.com/api/v10/channels/{CHANNEL_ID}
```

#### 必要な環境変数
- `DISCORD_BOT_TOKEN`: Discordボットトークン

#### 必要な権限
- Manage Channels (16)

#### 更新可能なプロパティ

**テキストチャンネル**:
- `name`: チャンネル名（2-100文字）
- `topic`: トピック（最大1024文字）
- `position`: ソート位置
- `parent_id`: カテゴリID
- `nsfw`: NSFWステータス
- `permission_overwrites`: 権限上書き

**ボイスチャンネル**:
- `name`: チャンネル名（2-100文字）
- `position`: ソート位置
- `parent_id`: カテゴリID
- `user_limit`: 最大ユーザー数（0-99、0=無制限）
- `bitrate`: ビットレート（8000-96000）
- `permission_overwrites`: 権限上書き

**カテゴリ**:
- `name`: カテゴリ名（2-100文字）
- `position`: ソート位置
- `permission_overwrites`: 権限上書き

#### 一般的な操作

**名前変更**:
```json
{
  "name": "new-channel-name"
}
```

**トピック更新**:
```json
{
  "topic": "New channel description"
}
```

**カテゴリ移動**:
```json
{
  "parent_id": "123456789012345678"
}
```

**NSFWトグル**:
```json
{
  "nsfw": true
}
```

**ボイス設定更新**:
```json
{
  "user_limit": 10,
  "bitrate": 64000
}
```

#### 権限上書き

```json
{
  "permission_overwrites": [
    {
      "id": "role_or_user_id",
      "type": 0,           // 0=ロール、1=ユーザー
      "allow": "1024",     // 許可する権限
      "deny": "2048"       // 拒否する権限
    }
  ]
}
```

**一般的な権限ビット**:
- `1024` (0x400): VIEW_CHANNEL
- `2048` (0x800): SEND_MESSAGES
- `8192` (0x2000): MANAGE_MESSAGES
- `65536` (0x10000): READ_MESSAGE_HISTORY
- `1048576` (0x100000): CONNECT (ボイス)
- `2097152` (0x200000): SPEAK (ボイス)

#### チャンネル削除

**重要**: チャンネル削除は永続的で元に戻せない。

削除前の確認事項：
1. ユーザーに削除の意図を確認
2. すべてのメッセージが失われることを警告
3. 必要に応じてアーカイブを提案
4. チャンネルIDが正しいことを確認

```bash
curl -X DELETE "https://discord.com/api/v10/channels/{CHANNEL_ID}" \
  -H "Authorization: Bot ${DISCORD_BOT_TOKEN}"
```

#### エラーハンドリング
- **400 Bad Request**: 無効なパラメータ
- **401 Unauthorized**: 無効なボットトークン
- **403 Forbidden**: Manage Channels権限不足
- **404 Not Found**: チャンネルが存在しない

#### レート制限
- 5リクエスト/5秒/チャンネル

---

## 共通パターン

### 1. 認証
すべてのスキルは`DISCORD_BOT_TOKEN`環境変数を使用：
```bash
-H "Authorization: Bot ${DISCORD_BOT_TOKEN}"
```

### 2. APIバージョン
すべてのエンドポイントがDiscord API v10を使用：
```
https://discord.com/api/v10/...
```

### 3. レート制限
ほとんどのエンドポイント：5リクエスト/5秒

### 4. エラーハンドリング
共通のHTTPステータスコード：
- **200 OK**: 成功
- **201 Created**: 作成成功
- **204 No Content**: 削除成功
- **400 Bad Request**: 無効なパラメータ
- **401 Unauthorized**: 無効なトークン
- **403 Forbidden**: 権限不足
- **404 Not Found**: リソースが存在しない

### 5. バリデーション
- チャンネルID: 18-19桁のsnowflake ID
- チャンネル名: 2-100文字、小文字、ハイフン/アンダースコアのみ
- メッセージコンテンツ: 最大2000文字

### 6. セキュリティ
- ボットトークンをログやメッセージに暴露しない
- ユーザー入力を検証してからDiscordに送信
- プライベート情報を慎重に扱う
- Discordのレート制限を尊重する

---

## 実装上の注意点

### 1. 環境変数の設定
```bash
export DISCORD_BOT_TOKEN="your-bot-token-here"
```

シェルプロファイルに追加して永続化：
```bash
echo 'export DISCORD_BOT_TOKEN="your-bot-token-here"' >> ~/.zshrc
source ~/.zshrc
```

### 2. ボットの権限
Discord Developer Portalでボットに以下の権限を付与：
- Send Messages (2048)
- Read Message History (65536)
- Manage Channels (16)
- View Channels (1024)

### 3. レート制限の尊重
連続リクエスト間に200ms〜1秒の遅延を追加：
```bash
curl ... && sleep 1 && curl ...
```

### 4. エラーメッセージ
ユーザーに明確でアクション可能なエラーメッセージを提供：
```
Error: Unable to send message. The bot is missing the "Send Messages" permission in this channel. Please check the bot's permissions in your Discord server.
```

---

## まとめ

agent-discord-skillsは、Claude Code用のDiscord APIスキルセットとして以下の特徴を持つ：

1. **シンプルな構造**: SKILL.md + examples.mdの2ファイル構成
2. **一貫したフォーマット**: YAML frontmatter + Markdown
3. **包括的なドキュメント**: 具体的な使用例とエラーハンドリング
4. **セキュリティ配慮**: トークン保護、バリデーション、権限管理
5. **ベストプラクティス**: レート制限、エラーハンドリング、ユーザーフィードバック

この構造は、Claude Code用のDiscordスキルを開発する際の優れたリファレンスとなる。
